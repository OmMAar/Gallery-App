import 'package:flutter/material.dart';

class MoMStatisticsModelFake {
  final String title;
  final String subTitle;
  final Color subTitleColor;
  final String count;

  const MoMStatisticsModelFake({required this.title,required this.subTitle,required this.count,required this.subTitleColor,});

}